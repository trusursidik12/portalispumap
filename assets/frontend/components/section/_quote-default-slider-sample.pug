include ../quote/_quote

// Quote default slider
section.section.section-lg.bg-transparent.novi-background( data-preset!={ title:'Quote Default Slider', category:'quote, carousel', reload:true, id:'quote-default-slider' } )
	.container
		h2.text-center Testimonials slider type #1
		// Owl Carousel
		//{DEL DIST BUILDER}
		// TODO заменить изображения для скрытых слайдов (другие имена)
		//{DEL}
		.owl-carousel.owl-style-2( data-owl= { dots: true }, data-items='1', data-sm-items='2', data-md-items='3' )
			+quote({
				text:     quote1.text,
				img:      'persons/person-01-64x64',
				name:     quote1.name,
				position: quote1.position
			})
			+quote({
				text:     quote2.text,
				img:      'persons/person-02-64x64',
				name:     quote2.name,
				position: quote2.position
			})
			+quote({
				text:     quote3.text,
				img:      'persons/person-03-64x64',
				name:     quote3.name,
				position: quote3.position
			})
			+quote({
				text:     quote4.text,
				img:      'persons/person-01-64x64',
				name:     quote4.name,
				position: quote4.position
			})
			+quote({
				text:     quote5.text,
				img:      'persons/person-02-64x64',
				name:     quote5.name,
				position: quote5.position
			})
			+quote({
				text:     quote6.text,
				img:      'persons/person-03-64x64',
				name:     quote6.name,
				position: quote6.position
			})
